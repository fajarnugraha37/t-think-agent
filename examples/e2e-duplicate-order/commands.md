# Suggested operator commands

```bash
make audit
make validate
make test

# Validate an individual phase with its documented example
cd skills/t-system-modeling
make validate test
```

For a real work item, replace each example path with the canonical artifact path under the work-item lifecycle directory.
