# Evidence Policy

Every mutation must have a change record. Every command must preserve command text, working directory, timestamps, exit code, and output digests. Every required verification must have a result. Never synthesize evidence.
