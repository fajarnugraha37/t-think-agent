# Coverage and traceability

Required path:

`Approved solution contract → PTGT → PLN → TST/RBK → implementation checklist → implementation evidence`

Every contract obligation is normalized into exactly one active planning target. Every mandatory target must have one coverage row with `FULL` coverage before transition to Plan Critique.
