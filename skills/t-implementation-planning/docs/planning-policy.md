# Planning policy

Implementation Planning converts an approved semantic contract into executable work decomposition. It is not allowed to edit implementation artifacts or reinterpret the approved behavior.

A plan item is complete only when it states: purpose, source mappings, exact affected surface, ordered operations, preconditions, failure conditions, verification, rollback, dependencies, and completion evidence.
