# Completeness Policy

Every mandatory dimension must appear exactly once, every change record exactly once, all source self-review checks must be covered, and the configured minimum evidence challenges must be met.
