# Environment and Evidence Policy

The environment fingerprint records the source tree, build artifact, toolchain, dependency versions, test data digest, clock, network controls, and secret handling. Every conclusion must resolve to digest-addressed `VE-*` evidence. Verification evidence must be reproducible unless an explicit limitation is recorded and routed.
