# Decision approval policy

Only the human may select and approve a solution. An agent recommendation remains non-binding. Approval is valid only when the selected option is active, viable, fully covers required targets, all blocking critiques are resolved, and all acceptance-required risks are explicitly accepted. Approval authorizes implementation planning, not implementation.
